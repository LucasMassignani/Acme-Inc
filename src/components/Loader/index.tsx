import React from 'react';

import { LoaderIcon, StyledDiv } from './styles';

export const Loader = (): React.ReactElement => {
  return (
    <StyledDiv>
      <LoaderIcon />
    </StyledDiv>
  );
};
